/** @todo  missing implementation. Check test for docs */

export const removeKeyList = <ResultingObject>(obj: { [key: string]: any }, keyList: string[]): ResultingObject => {
  return obj as ResultingObject;
}
